<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dunno.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.menu_fonction.MonFrigo.AddIngredient"></activity>
        <activity android:name=".ui.menu_fonction.MonFrigo.MonFrigoType" />
        <activity android:name=".ui.menu_fonction.MonFrigo.MonFrigoTypeChoice" />
        <activity android:name=".ui.menu_fonction.Account.AccountModifyActivity" />
        <activity android:name=".ui.menu_fonction.Account.AccountActivity" />
        <activity android:name=".ui.loginregister.RegisterActivity" />
        <activity
            android:name=".ui.loginregister.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".ui.menu.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>